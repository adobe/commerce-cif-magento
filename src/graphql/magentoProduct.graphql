{{#each simpleFields}}
{{this}}        
{{/each}}
{{#if priceFields}}
price {
    regularPrice {
        amount {
            {{#each priceFields}}
            {{this}}
            {{/each}}
        }
    }
}
{{/if}}
{{#if categories}}
categories {
    {{> product categories}}
}
{{/if}}
